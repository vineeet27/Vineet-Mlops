# DVC Pipeline Parameters

prepare:
  test_size: 0.2
  random_state: 42

train:
  model_params:
    max_iter: 200
    random_state: 42
    solver: 'lbfgs'
    multi_class: 'multinomial'

evaluate:
  metrics_output: 'metrics/metrics.json'
